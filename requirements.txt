streamlit>=1.29.0
pyspellchecker>=0.7.0

# Offline automatic speech-to-text (audio AND video/MP4 files).
# ffmpeg must also be installed on the system:
#   sudo apt install ffmpeg   (Linux)
#   brew install ffmpeg       (macOS)
vosk>=0.3.45
